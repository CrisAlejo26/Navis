/**
 * El saludo ahora vive en `@navis/shared` (RFC 0001: el panel de inicio saluda
 * igual en web y en móvil). Reexportado para no tocar quien ya lo importa de aquí.
 */
export { GREETING_KEY, greetingKeyFor, type GreetingKey } from '@navis/shared';
