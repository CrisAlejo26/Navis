{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permisos mínimos de la ventana principal. Se añade solo lo que se usa: cada permiso extra es superficie de ataque.",
  "windows": ["main"],
  "permissions": ["core:default", "os:allow-os-type", "os:allow-platform", "os:allow-version"]
}
