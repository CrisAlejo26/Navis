# Formatea y arregla lo que se va a subir, en vez de descubrirlo en CI.
#
# `lint-staged` (configurado en package.json) pasa oxlint, eslint y prettier
# **solo por los ficheros en el índice**, así que cuesta un par de segundos.
pnpm lint-staged
